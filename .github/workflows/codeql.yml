# file: .github/workflows/codeql.yml
# description: CodeQL analysis for Python and GitHub Actions
# reference: .github/workflows/ci.yml
name: codeql

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "27 4 * * 1"

permissions:
  contents: read
  security-events: write

concurrency:
  group: codeql-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        language: [python, actions]
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: github/codeql-action/init@615d2593c6a3a11f63fddff1723e85d5553dc1f5 # v3.29.9
        with:
          languages: ${{ matrix.language }}
      - uses: github/codeql-action/analyze@615d2593c6a3a11f63fddff1723e85d5553dc1f5 # v3.29.9
        with:
          category: "/language:${{ matrix.language }}"
